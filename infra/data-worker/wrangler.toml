name = "reflex-data"
main = "worker.js"
compatibility_date = "2026-05-01"

# Intentionally no D1 or R2 bindings. This legacy Worker is a 410 migration
# shim only; contribution-worker is the sole upload and statistics authority.

# Optional: bind a custom domain after first deploy.
# routes = [{ pattern = "data.fastcrest.com/*", custom_domain = true }]
